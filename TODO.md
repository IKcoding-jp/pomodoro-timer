# TODO

## 機能
- [x] 設定変更（作業時間・休憩時間を画面から変えられる）
- [x] 長い休憩（4セット後に15分）
- [x] タイマー終了時の通知音
- [x] セッション数のカウント表示

## デザイン
- [x] モダンで美しい見た目
- [ ] 残り時間を円グラフ（SVG）で表示するアニメーション

## コード品質
- [x] ネストが深い部分をリファクタリング（関数に切り出す）

## バグ修正
- [x] **影が出ていない**（style.css `.card`）：`box-shadow` の `rgba(0, 0, 0, 0,08)` が壊れている。最後がカンマ → `0.08`（ドット）に直す
- [x] **本文の文字色が効いていない**（style.css `body`）：`color: "1A1A1A"` は引用符付きで無効。`#1A1A1A` に直す
- [x] **休憩明けが必ず "25:00" に戻る**（script.js `handleTimerEnd` の else）：`display.textContent = "25:00"` が直書き。設定時間を反映するには `formatTime(timeLeft)` に変える
- [x] **ストップ後に再開すると作業時間が満タンに戻る**（script.js スタートボタンのクリック）：start側で毎回 `timeLeft = workInput.value * 60` しているため、一時停止からの再開ができない。ヒント：再開時は `timeLeft` を上書きしない分岐を作る
- [x] **休憩中にリセットすると状態がずれる**（script.js リセットボタン）：`timerStatus` を "work" に戻していない。リセット時に状態も初期化する

## 完了済み
- [x] 25分カウントダウン
- [x] 分:秒 形式表示
- [x] スタート/ストップ切り替え
- [x] リセット
- [x] 作業/休憩の自動切替
- [x] CSSレイアウト（Flexbox中央寄せ）
- [x] ボタンデザイン
